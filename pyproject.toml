[project]
name = "subimage-outpost"
version = "1.0.8"
description = "HTTP proxy with bearer token authentication"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.3",
    "httpx>=0.28.0",
    "uvicorn>=0.48.0",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest>=9.0.3",
]
