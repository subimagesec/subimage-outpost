[project]
name = "subimage-outpost"
version = "1.2.0"
description = "HTTP proxy with bearer token authentication"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.2",
    "httpx>=0.28.0",
    "uvicorn>=0.51.0",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest>=9.1.1",
]
