[project]
name = "subimage-outpost"
version = "1.1.0"
description = "HTTP proxy with bearer token authentication"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "httpx>=0.28.0",
    "uvicorn>=0.50.2",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest>=9.1.1",
]
