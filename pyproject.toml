[project]
name = "subimage-outpost"
version = "1.0.1"
description = "HTTP proxy with bearer token authentication"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.0",
    "httpx>=0.28.0",
    "uvicorn>=0.45.0",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest>=9.0.3",
]
